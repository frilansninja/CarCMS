package se.meltastudio.cms.parts.domain;

/**
 * Type of markup applied to part pricing.
 */
public enum MarkupType {
    NONE,       // No markup applied
    PERCENT,    // Percentage-based markup
    FIXED       // Fixed amount markup
}
